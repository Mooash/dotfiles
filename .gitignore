# Lots of junk goes in here.
/caches/
/backups/

# Top secret stuff!
/link/.ssh/authorized_keys
/link/.ssh/id_*
/link/.ssh/known_hosts

# Don't commit rbenv temp files.
/link/.rbenv/shims/
/link/.rbenv/versions/
/link/.rbenv/version

# Ignore any file with LOCAL in the name (good for per-machine vars/functions)
*LOCAL*

/conf/rescue_time_api_key

# Vim stuff
# Don't commit Vim plugins, vim-plug will install them.
/link/.vim/plugged
# Don't include our spelling files
/link/.vim/spell/
# Don't include local config
/link/.vimrc.local
/link/.vim/.netrwhist
/bin/subl
